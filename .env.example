# The call-back form backend: the Google Apps Script web app URL (see README, "Call-back form").
# Copy to .env.local for local builds; set the same variable in the host's settings.
VITE_LEAD_ENDPOINT=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
